import ResetPasswordPage from "@/components/ResetPassword/ResetPassword";
import { Suspense } from "react";

const Page = () => {
  return ( 
    <Suspense>
      <ResetPasswordPage />
    </Suspense>
   );
}
 
export default Page;